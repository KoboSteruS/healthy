# Настройка Telegram бота для сбора заявок

## Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Запуск системы

**Вариант A: Запуск Flask и бота вместе (рекомендуется для разработки)**

```bash
python run.py
```

**Вариант B: Запуск отдельно**

```bash
# Терминал 1 - Flask приложение
python app.py

# Терминал 2 - Telegram бот
python bot.py
```

### 3. Подписка на заявки

1. Найдите вашего бота в Telegram (имя бота можно узнать у @BotFather)
2. Отправьте команду `/start`
3. Бот ответит подтверждением подписки
4. Ваш `chat_id` автоматически сохранится в файл `chat_ids.json`

### 4. Проверка работы

1. Откройте сайт: http://localhost:5000
2. Заполните форму заказа
3. Отправьте заявку
4. Проверьте Telegram - заявка должна прийти всем подписчикам

## Структура работы

```
Клиент на сайте → Форма заказа → Flask API → Telegram Service → Все chat_id
```

## Файлы

- `bot.py` - Telegram бот (обрабатывает /start, сохраняет chat_id)
- `telegram_service.py` - Сервис для отправки заявок из Flask
- `chat_ids.json` - Файл с сохраненными chat_id (создается автоматически)
- `app.py` - Flask приложение (интегрировано с Telegram)

## Команды бота

- `/start` - подписаться на получение заявок
- `/status` - проверить статус подписки

## Формат уведомления о заявке

```
🆕 Новая заявка с сайта

👤 Имя: Иван Иванов
📞 Телефон: +79999999999
📦 Товар: Берёзовый веник
🔢 Количество: 3 шт.
💬 Комментарий: Доставить до 18:00
```

## Production запуск

Если используете Gunicorn для Flask:

```bash
# Терминал 1
gunicorn app:app

# Терминал 2
python bot.py
```

Или используйте systemd/supervisor для управления процессами.

## Переменные окружения

Создайте файл `.env`:

```env
TELEGRAM_BOT_TOKEN=your-bot-token-here
FLASK_ENV=production
SECRET_KEY=your-secret-key
```

Если `TELEGRAM_BOT_TOKEN` не указан, будет использован токен по умолчанию из кода.

## Логи

- `logs/app.log` - логи Flask приложения
- `logs/bot.log` - логи Telegram бота
- `logs/run.log` - логи при запуске через run.py

## Устранение неполадок

### Бот не отвечает

1. Проверьте, что токен бота правильный
2. Убедитесь, что бот запущен (`python bot.py`)
3. Проверьте логи в `logs/bot.log`

### Заявки не приходят в Telegram

1. Убедитесь, что есть подписчики (отправьте `/start` боту)
2. Проверьте файл `chat_ids.json` - там должны быть chat_id
3. Проверьте логи в `logs/app.log` на наличие ошибок

### Ошибка при отправке заявки

1. Проверьте интернет-соединение
2. Убедитесь, что токен бота действителен
3. Проверьте, что chat_id в файле корректные (числа)
