# Здоровый Пар - Лендинг на Flask

Лендинг для продажи натуральных веников из Карелии. Проект создан на Flask (Python).

## 🚀 Технологии

- **Backend**: Flask (Python)
- **Frontend**: HTML5, CSS3, JavaScript (ES6+)
- **Логирование**: Loguru
- **Telegram Bot**: pyTelegramBotAPI

## 📋 Требования

- Python 3.8+

## 🛠️ Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd "Healthy smoke"
```

### 2. Установка Python зависимостей

```bash
# Создайте виртуальное окружение
python -m venv venv

# Активируйте виртуальное окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

## 🏃 Запуск проекта

### Вариант 1: Запуск Flask и Telegram бота вместе

```bash
python run.py
```

Этот скрипт запустит одновременно Flask приложение и Telegram бота.

### Вариант 2: Запуск только Flask приложения

```bash
python app.py
```

Приложение будет доступно по адресу: http://localhost:5000

### Вариант 3: Запуск только Telegram бота

```bash
python bot.py
```

### Вариант 4: Запуск через Gunicorn (только Flask)

```bash
gunicorn app:app
```

**Важно:** При использовании Gunicorn Telegram бот нужно запускать отдельно в другом процессе.

## 📁 Структура проекта

```
.
├── app.py                 # Flask приложение
├── bot.py                 # Telegram бот
├── telegram_service.py     # Сервис для отправки заявок в Telegram
├── run.py                 # Скрипт для запуска Flask и бота вместе
├── requirements.txt       # Python зависимости
├── chat_ids.json          # Файл с chat_id подписчиков (создается автоматически)
├── templates/             # HTML шаблоны
│   ├── base.html
│   ├── index.html
│   └── components/
│       ├── header.html
│       ├── hero.html
│       ├── about.html
│       ├── owner.html
│       ├── products.html
│       ├── footer.html
│       └── order_form.html
├── static/               # Статические файлы
│   ├── css/
│   │   └── main.css      # Основные стили
│   └── js/
│       └── main.js       # JavaScript логика
└── logs/                 # Логи приложения
```

## 🔧 Конфигурация

### Переменные окружения

Создайте файл `.env` в корне проекта (опционально):

```env
FLASK_ENV=development
SECRET_KEY=your-secret-key-here
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
```

Если переменная `TELEGRAM_BOT_TOKEN` не установлена, будет использован токен по умолчанию из кода.

### Настройка Telegram бота

1. **Создайте бота через @BotFather в Telegram:**
   - Отправьте `/newbot` в @BotFather
   - Следуйте инструкциям и получите токен бота
   - Добавьте токен в переменную окружения `TELEGRAM_BOT_TOKEN` или в код

2. **Подписка на заявки:**
   - Найдите вашего бота в Telegram
   - Отправьте команду `/start`
   - Ваш `chat_id` будет автоматически сохранен в файл `chat_ids.json`

3. **Получение заявок:**
   - Все заявки с сайта автоматически отправляются всем подписчикам
   - Заявки приходят в формате HTML с информацией о заказе

**Команды бота:**
- `/start` - подписаться на получение заявок
- `/status` - проверить статус подписки

## 📝 API Endpoints

### POST /api/order

Отправка формы заказа.

**Тело запроса:**
```json
{
    "name": "Иван Иванов",
    "phone": "+79999999999",
    "product": "Берёзовый веник - 350 руб.",
    "quantity": "3",
    "comment": "Комментарий к заказу"
}
```

**Ответ (успех):**
```json
{
    "success": true,
    "message": "Заявка успешно отправлена! Мы свяжемся с вами в ближайшее время."
}
```

**Ответ (ошибка):**
```json
{
    "success": false,
    "error": "Описание ошибки"
}
```

## 🎨 Дизайн

Дизайн полностью соответствует оригинальному React проекту:
- Цветовая схема: зеленый (#7a8450), бежевый (#e6dcc8)
- Адаптивная верстка (mobile-first)
- Плавные анимации и переходы
- Модальное окно для заказа

## 🧪 Тестирование

Для тестирования формы заказа:

1. Откройте лендинг в браузере
2. Нажмите кнопку "Оформить заказ"
3. Заполните форму
4. Отправьте заявку
5. Проверьте логи в `logs/app.log`

## 📦 Деплой

### Production настройки

1. Установите переменную окружения:
```bash
export FLASK_ENV=production
export SECRET_KEY=your-production-secret-key
```

2. Используйте production WSGI сервер (Gunicorn):

**Простой запуск:**
```bash
gunicorn app:app
```

**С параметрами:**
```bash
gunicorn -w 4 -b 0.0.0.0:5000 app:app
```

**С использованием конфигурационного файла:**
```bash
gunicorn -c gunicorn.conf.py app:app
```

**Переменные окружения:**
- `PORT` - порт для запуска (по умолчанию 5000)
- `FLASK_ENV` - режим работы (development/production)
- `LOG_LEVEL` - уровень логирования (debug/info/warning/error)

## 🤖 Telegram бот

Бот автоматически собирает заявки с сайта и отправляет их всем подписчикам.

### Как это работает:

1. Пользователь отправляет `/start` боту → его `chat_id` сохраняется
2. Клиент заполняет форму на сайте → заявка отправляется
3. Заявка автоматически приходит всем подписчикам в Telegram

### Формат уведомления о заявке:

```
🆕 Новая заявка с сайта

👤 Имя: Иван Иванов
📞 Телефон: +79999999999
📦 Товар: Берёзовый веник
🔢 Количество: 3 шт.
💬 Комментарий: Доставить до 18:00
```

### Запуск бота отдельно:

Если вы используете Gunicorn для Flask, запустите бота в отдельном процессе:

```bash
# В первом терминале
gunicorn app:app

# Во втором терминале
python bot.py
```

Или используйте systemd/supervisor для управления процессами.

## 📄 Лицензия

Проект создан для коммерческого использования.

## 👨‍💻 Разработка

### Добавление новых секций

1. Создайте HTML файл в `templates/components/`
2. Добавьте include в `templates/index.html`
3. Добавьте стили в `static/css/main.css` при необходимости

### Изменение цветов

Измените CSS переменные в начале файла `static/css/main.css`:

```css
:root {
    --color-primary: #7a8450;
    --color-primary-dark: #6a7440;
    /* ... */
}
```

## 📞 Контакты

- Телефон: +7 (999) 999-99-99
- Email: info@zdoroviypar.ru
- Telegram: @zdoroviypar

---

Сделано с ❤️ для любителей бани
  